<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.karan.myapplication"  >
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name="ParseApplication"
        android:icon="@drawable/abc"
        >
        <activity
            android:name=".MainActivity"
           >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginSignupActivity" >
        </activity>
        <activity android:name=".Welcome" android:theme="@style/Theme.AppCompat.Light"
        android:label="@string/Home"/>
        <activity android:name=".LocationActivity" android:theme="@style/Theme.AppCompat.Light">
    </activity>
        <activity android:name=".AddGroup" android:theme="@style/Theme.AppCompat.Light"
            android:label="@string/Groups">

        </activity>
    </application>

</manifest>
